# Custom Instructions

This manifest replaces the default instruction set with your own fragments.

List the fragments you want in `manifest.json` (in order, with optional `when`
gates). If your EMR has particular quirks or requirements, specify them here so
the MCP server handles them correctly.
